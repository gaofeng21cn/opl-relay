interface:
  display_name: "Codex Mail Workbench"
  short_description: "Use local mail, private memory, and drafting context safely"
  default_prompt: "Use $codex-mail-workbench to inspect configured mailboxes, retrieve approved private memory and knowledge context, and preserve explicit freshness, evidence, and review gates."
