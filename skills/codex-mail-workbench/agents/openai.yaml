interface:
  display_name: "Codex Mail Workbench"
  short_description: "Audit mailbox coverage, freshness, and evidence"
  default_prompt: "Use $codex-mail-workbench to audit configured mailboxes over an explicit date range, report per-account freshness and review coverage, and route requested follow-up actions through OPL Relay."
